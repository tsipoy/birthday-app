{
  "name": "birthday-app",
  "version": "1.0.0",
  "description": "- Fecth bootstrap through a link in the html.\r - Grab few elements from html.\r - Fetch the data from the `people.json`.\r - Store the data in the `local storage`.\r - Populate these data using html. Inside that population, I format the birthday date. After, I call that function in another new function.\r - Then, I generate the edit(open modal). So, I create editPopup function, editAddPopup fuction and add event listener for the input to be submitted.\r - When they are finished, I create the delete function with its confirm popup, the add event listener too.\r - Then, I put the some events listers at the end.\r - I wrap all the function inside a big `async`.",
  "main": "script.js",
  "dependencies": {
    "bootstrap": "^4.5.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tsipoy/birthday-app.git"
  },
  "author": "",
  "license": "ISC",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "bugs": {
    "url": "https://github.com/tsipoy/birthday-app/issues"
  },
  "homepage": "https://github.com/tsipoy/birthday-app#readme"
}
